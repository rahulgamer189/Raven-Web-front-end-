function getBroadcasts(){return JSON.parse(localStorage.getItem("raven_broadcasts")||"null")||window.RAVEN_DATA.broadcasts}
function saveBroadcast(b){const list=getBroadcasts();list.unshift(b);localStorage.setItem("raven_broadcasts",JSON.stringify(list.slice(0,30)));return list}
function renderBroadcastHistory(){const el=document.getElementById("broadcast-history");if(!el)return;el.innerHTML=getBroadcasts().map(b=>`<div class="feed-item"><div class="feed-time">${escapeHTML(b.time)}</div><div class="feed-body"><strong>${escapeHTML(b.title)} <span class="risk risk-${escapeHTML(b.severity.toLowerCase())}">${escapeHTML(b.severity)}</span></strong><span>${escapeHTML(b.message)}</span><small>${escapeHTML(b.area)}</small></div></div>`).join("")}
function renderLatestBroadcast(){const el=document.getElementById("latest-broadcast");if(!el)return;const b=getBroadcasts()[0];el.innerHTML=b?`<div class="latest"><h3>${escapeHTML(b.title)}</h3><p>${escapeHTML(b.message)}</p><small>${escapeHTML(b.area)} • ${escapeHTML(b.time)} • ${escapeHTML(b.severity)}</small></div>`:"<p class='muted'>No active broadcast.</p>"}
document.addEventListener("DOMContentLoaded",()=>{renderBroadcastHistory();renderLatestBroadcast();const f=document.getElementById("broadcast-form");if(f)f.addEventListener("submit",e=>{e.preventDefault();const d=new FormData(f);saveBroadcast({id:"B-"+Date.now(),title:d.get("title"),message:d.get("message"),severity:d.get("severity"),area:d.get("area"),time:"JUST NOW"});f.reset();renderBroadcastHistory();renderLatestBroadcast();ravenToast("Emergency broadcast published")})});
