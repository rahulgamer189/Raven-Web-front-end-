let activeSOS=JSON.parse(localStorage.getItem("raven_active_sos")||"null");
function renderSOS(){const status=document.getElementById("sos-status"),send=document.getElementById("send-sos"),cancel=document.getElementById("cancel-sos");if(!status)return;if(activeSOS){status.innerHTML=`<strong class="sos-active-text">Signal ${escapeHTML(activeSOS.id)} is active</strong><br>Sent ${escapeHTML(activeSOS.time)}${activeSOS.coords?` • ${activeSOS.coords}`:""}`;send.hidden=true;cancel.hidden=false}else{status.textContent="Ready to send an emergency signal.";send.hidden=false;cancel.hidden=true}}
function sendSOS(){const f=document.getElementById("sos-form"),data=new FormData(f),finish=coords=>{activeSOS={id:"SOS-"+Date.now().toString().slice(-5),time:new Date().toLocaleTimeString(),coords:coords?`${coords.latitude.toFixed(4)}, ${coords.longitude.toFixed(4)}`:"Location unavailable",note:data.get("note"),meeting:data.get("meeting")};localStorage.setItem("raven_active_sos",JSON.stringify(activeSOS));renderSOS();ravenToast("SOS signal sent to the Raven network","error")};if(navigator.geolocation)navigator.geolocation.getCurrentPosition(p=>finish(p.coords),()=>finish(null),{timeout:5000});else finish(null)}
document.addEventListener("DOMContentLoaded",()=>{renderSOS();document.getElementById("send-sos")?.addEventListener("click",sendSOS);document.getElementById("cancel-sos")?.addEventListener("click",()=>{if(confirm("Withdraw the active SOS signal?")){activeSOS=null;localStorage.removeItem("raven_active_sos");renderSOS();ravenToast("SOS signal withdrawn")}})});
