function getReports(){return JSON.parse(localStorage.getItem("raven_reports")||"null")||window.RAVEN_DATA.reports}
function saveReport(r){const list=getReports();list.unshift(r);localStorage.setItem("raven_reports",JSON.stringify(list.slice(0,50)))}
function renderReports(){const el=document.getElementById("reports-list");if(!el)return;const filter=document.getElementById("report-filter")?.value||"all";const list=getReports().filter(r=>filter==="all"||r.severity===filter);el.innerHTML=list.map(r=>`<article class="report"><div class="report-top"><strong>${escapeHTML(r.location)}</strong><span class="risk risk-${escapeHTML(r.severity.toLowerCase())}">${escapeHTML(r.severity)}</span></div><p>${escapeHTML(r.condition)}</p><small>${escapeHTML(r.resources||"No resource request")}${r.photo?" • 📷 "+escapeHTML(r.photo):""} • ${escapeHTML(r.time)}</small></article>`).join("")||"<p class='muted'>No reports match the filter.</p>"}
document.addEventListener("DOMContentLoaded",()=>{renderReports();document.getElementById("report-filter")?.addEventListener("change",renderReports);document.getElementById("report-form")?.addEventListener("submit",e=>{e.preventDefault();const d=new FormData(e.target),photo=d.get("photo");saveReport({id:"FR-"+Date.now(),location:d.get("location"),condition:d.get("condition"),resources:d.get("resources"),severity:d.get("severity"),photo:photo?.name||"",time:"JUST NOW"});e.target.reset();renderReports();ravenToast("Field report submitted")})});
